services:
  backend:
    build: ./app/backend
    env_file: ./app/backend/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./app/frontend
    ports:
      - "80:80"
    depends_on:
      - backend
